import starlight from '@astrojs/starlight'
import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://pi-memsearch.espadat.com',
  integrations: [
    starlight({
      title: 'pi-memsearch',
      description:
        'Long-term memory for pi — semantic recall over a memsearch store shared with Claude Code, Codex and OpenCode.',
      logo: { src: './src/assets/mark.svg', alt: 'Espadat' },
      head: [{ tag: 'link', attrs: { rel: 'icon', href: '/favicon.ico', sizes: '16x16 32x32' } }],
      customCss: ['@espadat/docs-theme/styles/theme.css'],
      components: {
        Footer: '@espadat/docs-theme/components/footer.astro',
        ThemeProvider: '@espadat/docs-theme/components/theme-provider.astro',
        ThemeSelect: '@espadat/docs-theme/components/theme-select.astro',
      },
      social: [
        {
          icon: 'github',
          label: 'GitHub',
          href: 'https://github.com/espadat-studio/pi-memsearch',
        },
      ],
      // Written out rather than autogenerated, as ADR-0001 requires. One entry
      // today; #112-#115 move the README's sections in and extend it.
      sidebar: [{ label: 'Home', link: '/' }],
    }),
  ],
})
